import 'package:flutter/material.dart';
import 'package:iconsax/iconsax.dart';

class ConstIcons {
  final outlineFavorite = const Icon(
    Iconsax.heart,
    size: 14,
    color: Color(0xFF454545),
  );
  final filledFavorite = const Icon(
    Icons.favorite,
    size: 14,
    color: Color.fromARGB(255, 255, 30, 30),
  );

  final outlineHome = const Icon(
    Iconsax.home,
    size: 30,
    color: Color(0xFF292929),
  );
  final filledHome = const Icon(
     Iconsax.home1,
    size: 30,
    color: Color(0xFF292929),
  );

  final outlineFavoritesNaveBar = const Icon(
    Icons.favorite_outline,
    size: 30,
    color: Color(0xFF292929),
  );
  final filledFavoritesNaveBar = const Icon(
    Icons.favorite,
    size: 30,
    color: Color(0xFF292929),
  );

  final outlineMyCart = const Icon(
    Iconsax.shopping_cart,
    size: 30,
    color: Color(0xFF292929),
  );
  final filledMyCart = const Icon(
     Iconsax.shopping_cart5,
    size: 30,
    color: Color(0xFF292929),
  );

  final outlineProfile = const Icon(
    Icons.person_outlined,
    size: 30,
    color: Color(0xFF292929),
  );
  final filledProfile = const Icon(
    Icons.person,
    size: 30,
    color: Color(0xFF292929),
  );

  final add = const Icon(
    Iconsax.add,
    size:20,
    color: Colors.white,

  );
final added = const Icon(
    Icons.check,
    size:20,
    color: Colors.white,

  );

  final notification = const Icon(
    Iconsax.notification,
    size: 30,
    color: Color(0xFF292929),
  );

  final pin = const Icon(
    Iconsax.location5,
    size: 20,
    color: Color(0xFF292929),
  );

  final down = const Icon(
    Icons.keyboard_arrow_down_outlined,
    size: 20,
    color: Color(0xFF292929),
  );

  final right = const Icon(
    Icons.keyboard_arrow_right_outlined,
    size: 15,
    color: Color(0xFF292929),
  );

  final searchBar = const Icon(
    Iconsax.search_normal,
    size: 20,
    color: Color(0xFF5F5F5F),
  );
  final setting = const Icon(
    Iconsax.setting_44,
    size: 25,
    color: Color(0xFF5F5F5F),
  );


}
